import styled from 'styled-components';

const AnswerSelectHint = styled.div`
  position: absolute;
  height: 100%;
  width: 2rem;
  transform: translateX(-3rem) translateY(-1.5rem);
  background-color: red;
`;

export default AnswerSelectHint;
